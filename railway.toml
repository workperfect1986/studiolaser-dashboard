[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn --chdir dashboard -b 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 180 --access-logfile - --error-logfile - server:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
